name: turbodbc
channels:
  - conda-forge
dependencies:
  - python>=3.8
  - pyarrow>=1.0,<11
  - sel(unix): unixodbc
  - c-compiler
  - cxx-compiler
  - gmock=1.10.0
  - gtest
  - boost-cpp
  - cmake
  - pkg-config
  - ninja
  - pytest
  - numpy
  - pybind11
